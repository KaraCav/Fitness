window.alert("Welcome")


function addText() {
	var log=prompt("Describe your workout");
	this.innerHTML(log);
}

/*Put img grayCircle to the right of all calendar days with #s. Write an
open (style.display="block") and a closed (style.display="none") function
and have the default be closed. Call the open code when there is a log entry.
See end of Unit 02.05 in JS course for help */
